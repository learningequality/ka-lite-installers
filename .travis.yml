language: python
sudo: required
install: true
script:
 - debian/test.sh
 - kalite
addons:
  apt:
    packages:
    - build-essential
    - realpath
    - devscripts
    - python-setuptools
    - python-all
    - unzip
    - debconf
    - bash
    - wget
    - dh-exec
